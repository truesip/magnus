# MagnusBilling API Configuration
MAGNUSBILLING_URL=https://sip.dialerone.net/mbilling
# Use '/user' by default. If your install expects '/api/user', set that here.
MAGNUSBILLING_USER_PATH=/user
# If your base URL omits '/api', set path to '/api/user'. Example when using IP base like https://1.2.3.4/mbilling
# MAGNUSBILLING_USER_PATH=/api/user
MAGNUSBILLING_API_KEY=your_api_key_here
MAGNUSBILLING_API_SECRET=your_api_secret_here

# TLS / Host settings (optional; prefer valid certificates in production)
# Use when connecting via IP to a server with a domain certificate
# MAGNUSBILLING_TLS_SERVERNAME=sip.example.com
# Set to 1 only for temporary testing to skip TLS verification
# MAGNUSBILLING_TLS_INSECURE=1
# When connecting to an IP but routing to a vhost, set Host header to the domain
# MAGNUSBILLING_HOST_HEADER=sip.example.com

# Default Group and Plan IDs
DEFAULT_GROUP_ID=3
DEFAULT_PLAN_ID=1
# Optional: concurrent call limit for new users
# DEFAULT_CALL_LIMIT=5

# Email verification via SMTP2GO
# SMTP2GO_API_KEY=your_api_key_here
# SMTP2GO_SENDER=no-reply@talkusa.net
# EMAIL_VERIFICATION_TTL_MINUTES=10

# Server Configuration
PORT=8080
NODE_ENV=production
# Set DEBUG=1 to enable verbose logging (recommended while diagnosing 404s)
# DEBUG=1

# App session secret (REQUIRED for login sessions)
# SESSION_SECRET=change_me

# Optional overrides if your MagnusBilling uses different module names or filters
# MB_SIP_MODULE=sip
# MB_CDR_MODULE=cdr
# MB_PAGE_SIZE=50

# TalkUSA DID markup pricing (monthly, in USD)
# Local DIDs will be billed this amount per month (per number)
# DID_LOCAL_MONTHLY_MARKUP=10.20
# Toll-free DIDs will be billed this amount per month (per number)
# DID_TOLLFREE_MONTHLY_MARKUP=25.20

# NOWPayments configuration (crypto payment gateway)
# NOWPAYMENTS_API_KEY=your_nowpayments_api_key
# NOWPAYMENTS_PUBLIC_KEY=your_nowpayments_public_key
# NOWPAYMENTS_IPN_SECRET=your_nowpayments_ipn_secret
# PUBLIC_BASE_URL=https://portal.yourdomain.com

# ADMIN_TOKEN=change_me_admin_token
# COOKIE_SECURE=0
